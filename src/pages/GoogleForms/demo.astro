---
// Demo Google Forms API Integration
// Based on the README guide for exploiting Google Forms as a free backend
---

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🚀 Custom Google Forms Demo</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 100%;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #333;
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .header p {
            color: #666;
            font-size: 1.1rem;
        }

        .form-group {
            margin-bottom: 25px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
            font-size: 1rem;
        }

        input[type="text"],
        input[type="number"],
        select {
            width: 100%;
            padding: 12px 16px;
            border: 2px solid #e1e5e9;
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: #f8f9fa;
        }

        input[type="text"]:focus,
        input[type="number"]:focus,
        select:focus {
            outline: none;
            border-color: #667eea;
            background: white;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .checkbox-group {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .checkbox-item {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .checkbox-item input[type="checkbox"] {
            width: 18px;
            height: 18px;
            accent-color: #667eea;
        }

        .checkbox-item label {
            margin-bottom: 0;
            font-weight: 500;
            cursor: pointer;
        }

        .submit-btn {
            width: 100%;
            padding: 15px;
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 20px;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
        }

        .submit-btn:active {
            transform: translateY(0);
        }

        .success-message {
            background: #d4edda;
            color: #155724;
            padding: 15px;
            border-radius: 10px;
            margin-top: 20px;
            display: none;
            text-align: center;
        }

        .error-message {
            background: #f8d7da;
            color: #721c24;
            padding: 15px;
            border-radius: 10px;
            margin-top: 20px;
            display: none;
            text-align: center;
        }

        .loading {
            opacity: 0.6;
            pointer-events: none;
        }

        .tech-info {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-top: 30px;
            border-left: 4px solid #667eea;
        }

        .tech-info h3 {
            color: #333;
            margin-bottom: 10px;
            font-size: 1.1rem;
        }

        .tech-info p {
            color: #666;
            font-size: 0.9rem;
            line-height: 1.6;
        }

        .tech-info code {
            background: #e9ecef;
            padding: 2px 6px;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
        }

        @media (max-width: 600px) {
            .container {
                padding: 30px 20px;
            }
            
            .header h1 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚀 Custom Google Forms Demo</h1>
            <p>Exploiting Google Forms API as Free Backend</p>
        </div>

        <form id="customForm" method="POST" action="https://docs.google.com/forms/d/e/1FAIpQLSean50NEFqYQSnhrRi9rH9m87F9lxwX-8IKZ-5XTCAu-_HkZg/formResponse" target="hidden_iframe">
            <div class="form-group">
                <label for="field1">📊 Number Field 1:</label>
                <input type="number" id="field1" name="entry.1418512851" placeholder="Enter a number..." required>
            </div>

            <div class="form-group">
                <label for="field2">📈 Number Field 2:</label>
                <input type="number" id="field2" name="entry.1294658972" placeholder="Enter another number..." required>
            </div>

            <div class="form-group">
                <label for="field3">✏️ Text Field:</label>
                <input type="text" id="field3" name="entry.1536065241" placeholder="Enter some text..." required>
            </div>

            <div class="form-group">
                <label>🎯 Multiple Choice Options:</label>
                <div class="checkbox-group">
                    <div class="checkbox-item">
                        <input type="checkbox" id="optionA" name="entry.1171854648" value="Option A">
                        <label for="optionA">Option A</label>
                    </div>
                    <div class="checkbox-item">
                        <input type="checkbox" id="optionB" name="entry.1171854648" value="Option B">
                        <label for="optionB">Option B</label>
                    </div>
                    <div class="checkbox-item">
                        <input type="checkbox" id="optionC" name="entry.1171854648" value="Option C">
                        <label for="optionC">Option C</label>
                    </div>
                </div>
            </div>

            <!-- Honeypot field for spam prevention -->
            <input type="text" name="honeypot" style="display: none;">

            <button type="submit" class="submit-btn">
                🚀 Submit to Google Forms
            </button>
        </form>

        <div id="successMessage" class="success-message">
            ✅ Form submitted successfully! Data has been sent to Google Sheets.
        </div>

        <div id="errorMessage" class="error-message">
            ❌ Something went wrong. Please try again.
        </div>

        <!-- Hidden iframe for form submission -->
        <iframe name="hidden_iframe" style="display: none;"></iframe>

        <div class="tech-info">
            <h3>🔧 Technical Details:</h3>
            <p>
                This form uses the Google Forms API "backdoor" technique described in the README. 
                Data is sent directly to <code>formResponse</code> endpoint, bypassing Google's UI 
                while maintaining all backend functionality including automatic Google Sheets storage.
            </p>
            <p style="margin-top: 10px;">
                <strong>Entry IDs:</strong> <code>1418512851</code>, <code>1294658972</code>, 
                <code>1536065241</code>, <code>1171854648</code>
            </p>
        </div>
    </div>

    <script>
        document.getElementById('customForm').addEventListener('submit', function(e) {
            const form = e.target;
            const container = document.querySelector('.container');
            const successMessage = document.getElementById('successMessage');
            const errorMessage = document.getElementById('errorMessage');
            
            // Check honeypot field
            const honeypot = form.querySelector('input[name="honeypot"]');
            if (honeypot.value !== '') {
                e.preventDefault();
                errorMessage.style.display = 'block';
                successMessage.style.display = 'none';
                return;
            }
            
            // Add loading state
            container.classList.add('loading');
            successMessage.style.display = 'none';
            errorMessage.style.display = 'none';
            
            // Handle form submission
            setTimeout(() => {
                container.classList.remove('loading');
                successMessage.style.display = 'block';
                errorMessage.style.display = 'none';
                
                // Optional: Reset form after successful submission
                setTimeout(() => {
                    form.reset();
                }, 2000);
            }, 1000);
        });

        // Real-time validation
        document.querySelectorAll('input[required]').forEach(input => {
            input.addEventListener('blur', function() {
                if (!this.value.trim()) {
                    this.style.borderColor = '#dc3545';
                } else {
                    this.style.borderColor = '#28a745';
                }
            });
        });

        // Progressive enhancement: Add smooth animations
        document.querySelectorAll('.form-group').forEach((group, index) => {
            group.style.opacity = '0';
            group.style.transform = 'translateY(20px)';
            
            setTimeout(() => {
                group.style.transition = 'all 0.5s ease';
                group.style.opacity = '1';
                group.style.transform = 'translateY(0)';
            }, index * 100);
        });
    </script>
</body>
</html>
